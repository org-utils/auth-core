{
  "name": "auth-core-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean:dist": "rimraf packages/*/dist",
    "clean": "rimraf packages/*/dist packages/*/src/**/*.d.ts packages/*/src/**/*.d.mts packages/*/src/**/*.d.cts packages/*/src/**/*.tsbuildinfo",
    "build:types": "tsc -b --force",
    "build:js": "npm run build --workspaces",
    "build": "npm run clean && npm run build:types && npm run build:js",
    "test": "vitest",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "clean:git": "git clean -xdf",
    "changeset": "changeset",
    "version": "changeset version",
    "publish": "changeset publish",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@types/node": "^22.9.0",
    "rimraf": "^6.1.3",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  },
  "version": "",
  "allowScripts": {
    "argon2": true,
    "bcrypt": true,
    "esbuild": true
  }
}
