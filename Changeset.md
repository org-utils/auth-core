first run command
```bash
npm run changeset
```
after that, run `git push` to trigger the release workflow.
then, when the PR is merged, the release will be automatically created.
just make sure to merge the PR before the release is created.
and automatically the new version will be published to npm.
